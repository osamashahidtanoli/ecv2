import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3d5a80",
    },
    secondary: {
      main: "#d2e2f8",
    },
  },
});

export default theme;
